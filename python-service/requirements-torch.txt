# Install AFTER requirements.txt.
# CPU-only torch wheel — safe for AMD GPUs on Windows (ROCm on Windows is limited).
# --extra-index-url keeps PyPI as primary so ultralytics resolves correctly;
# torch/torchvision pull their CPU wheels from the PyTorch index.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0
torchvision>=0.18.0
ultralytics>=8.2.0
