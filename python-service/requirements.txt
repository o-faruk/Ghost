fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
anthropic>=0.28.0
pillow>=10.3.0
python-dotenv>=1.0.0

# OmniParser v2 has heavy deps (torch, transformers, groundingdino-py).
# Install those separately following the OmniParser repo instructions after cloning it.
# Do NOT add them here — they require a CUDA/CPU-specific torch wheel and will vary by machine.
